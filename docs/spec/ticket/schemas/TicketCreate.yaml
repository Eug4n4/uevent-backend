type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
      - relationships
    properties:
      type:
        type: string
        enum: [ticket]
      attributes:
        type: object
        required:
          - name
          - price
          - currency
          - total
        properties:
          name:
            type: string
            maxLength: 255
            example: VIP
          description:
            type: string
            maxLength: 255
            example: Front row access
          price:
            type: number
            format: double
            minimum: 0
            example: 49.99
          currency:
            type: string
            minLength: 3
            maxLength: 3
            example: USD
          total:
            type: integer
            minimum: 1
            description: Total number of tickets available
            example: 100
      relationships:
        type: object
        required:
          - event
        properties:
          company:
            type: object
            required:
              - data
            properties:
              data:
                type: object
                required:
                  - id
                  - type
                properties:
                  id:
                    type: string
                    format: uuid
                    description: "ID of the event hosting the event"
                  type:
                    type: string
                    enum: [ event ]
