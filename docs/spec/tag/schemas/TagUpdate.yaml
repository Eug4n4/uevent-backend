type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "must match the :id in the URL"
      type:
        type: string
        enum: [ tag ]
      attributes:
        type: object
        properties:
          name:
            type: string
            description: "tag name (letters only, max 50 characters)"
            example: "Technology"
          description:
            type: string
            description: "optional tag description"
            example: "Tech-related events"
