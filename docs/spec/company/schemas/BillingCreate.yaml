type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [billing]
      attributes:
        type: object
        required:
          - stripe_account_id
        properties:
          stripe_account_id:
            type: string
            description: Stripe connected account ID
            example: acct_1234567890
